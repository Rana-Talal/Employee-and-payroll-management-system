# دليل التكامل مع API

## نظرة عامة

تم إنشاء طبقة خدمات API مركزية لتسهيل التكامل مع Backend API الموجود على `http://192.168.11.230:1006/api`

---

## 📁 الملفات المُنشأة

### 1. ملف الخدمات المركزي
**المسار:** `src/services/api.js`

يحتوي على:
- ✅ دوال GET, POST, PUT, DELETE عامة
- ✅ معالجة تلقائية للمصادقة (Bearer Token)
- ✅ معالجة الأخطاء المركزية
- ✅ إعادة التوجيه التلقائية عند انتهاء الجلسة (401)
- ✅ جميع endpoints من API

### 2. ملف تعريفات الأنواع
**المسار:** `src/services/types.js`

يحتوي على:
- ✅ JSDoc type definitions لجميع النماذج
- ✅ DTOs (Data Transfer Objects)
- ✅ Response Models
- ✅ Enums للقيم الثابتة

### 3. صفحات جديدة

#### أ. صفحة كتب الشكر والترفيعات
**المسار:** `src/pages/AppreciationLetters.jsx`

**الميزات:**
- ✅ عرض جميع الكتب مع pagination
- ✅ إضافة كتاب شكر/ترفيع جديد
- ✅ تعديل كتب موجودة
- ✅ حذف كتب
- ✅ بحث وتصفية متقدمة
- ✅ واجهة مستخدم عصرية

**الرابط في النظام:** `/appreciation-letters`

#### ب. صفحة إدارة الدورات التدريبية
**المسار:** `src/pages/CoursesManagement.jsx`

**الميزات:**
- ✅ عرض الدورات بشكل Cards جميلة
- ✅ إضافة دورة جديدة
- ✅ تعديل الدورات
- ✅ حذف الدورات
- ✅ تسجيل الموظفين في الدورات
- ✅ عرض معلومات تفصيلية (المدرس، النوع، المستوى، التواريخ)

**الرابط في النظام:** `/courses` (محدّث من CourseList القديم)

---

## 🔧 كيفية الاستخدام

### استيراد خدمات API

```javascript
import api from '../services/api';
```

### أمثلة الاستخدام

#### 1. جلب قائمة الموظفين
```javascript
const employees = await api.employees.getAll({ PageSize: 100 });
```

#### 2. إضافة كتاب شكر
```javascript
const newLetter = await api.appreciationLetters.create({
  employeeID: 123,
  letterType: 0,
  period: 1,
  reason: 'أداء متميز',
  adminOrderDate: '2026-01-18T00:00:00Z',
  adminOrderNumber: 'A-2026-001',
  periodOrderDate: null,
  periodOrderNumber: null
});
```

#### 3. تسجيل موظف في دورة
```javascript
await api.courses.enrollEmployee({
  courseId: 5,
  employeeId: 123,
  notes: 'ممتاز',
  degree: 95
});
```

#### 4. جلب مخصصات موظف
```javascript
const entitlements = await api.entitlements.getByEmployee(employeeId);
```

#### 5. احتساب راتب موظف
```javascript
await api.salaries.generate({
  employeeID: 123,
  month: 1,
  year: 2026,
  notes: 'راتب يناير 2026'
});
```

---

## 📚 قائمة الخدمات المتاحة

### 1. **appreciationLettersAPI**
- `getAll(params)` - جلب جميع الكتب
- `getById(id)` - جلب كتاب محدد
- `create(data)` - إضافة كتاب جديد
- `update(id, data)` - تحديث كتاب
- `delete(id)` - حذف كتاب

### 2. **employeesAPI**
- `getAll(params)` - جلب جميع الموظفين
- `getById(id)` - جلب موظف محدد
- `create(data)` - إضافة موظف جديد
- `update(id, data)` - تحديث بيانات موظف
- `delete(id)` - حذف موظف

### 3. **coursesAPI**
- `getAll(params)` - جلب جميع الدورات
- `getById(id)` - جلب دورة محددة
- `create(data)` - إضافة دورة جديدة
- `update(id, data)` - تحديث دورة
- `delete(id)` - حذف دورة
- `enrollEmployee(data)` - تسجيل موظف في دورة

### 4. **entitlementsAPI**
- `getAllTypes(params)` - جلب أنواع المخصصات
- `getByEmployee(employeeId)` - جلب مخصصات موظف
- `assign(data)` - إضافة مخصصات لموظف
- `update(id, data)` - تحديث مخصصات
- `delete(id)` - حذف مخصصات

### 5. **deductionsAPI**
- `getAllTypes(params)` - جلب أنواع الاستقطاعات
- `getByEmployee(employeeId)` - جلب استقطاعات موظف
- `assign(data)` - إضافة استقطاعات لموظف
- `update(id, data)` - تحديث استقطاعات
- `delete(id)` - حذف استقطاعات

### 6. **serviceCalculationAPI**
- `getByEmployee(employeeId)` - جلب سجلات الخدمة
- `add(data)` - إضافة سجل خدمة
- `update(id, data)` - تحديث سجل خدمة
- `delete(id)` - حذف سجل خدمة

### 7. **branchesAPI**
- `getAll(params)` - جلب جميع الفروع
- `getById(id)` - جلب فرع محدد
- `create(data)` - إضافة فرع جديد
- `update(id, data)` - تحديث فرع
- `delete(id)` - حذف فرع

### 8. **departmentsAPI**
- `getAll(params)` - جلب جميع الأقسام
- `getByBranch(branchId)` - جلب أقسام فرع محدد
- `getById(id)` - جلب قسم محدد
- `create(data)` - إضافة قسم جديد
- `update(id, data)` - تحديث قسم
- `delete(id)` - حذف قسم

### 9. **unitsAPI**
- `getAll(params)` - جلب جميع الوحدات
- `create(data)` - إضافة وحدة جديدة
- `update(id, data)` - تحديث وحدة
- `delete(id)` - حذف وحدة

### 10. **salaryScaleAPI**
- `getAll(params)` - جلب سلم الرواتب
- `getByGradeStep(grade, step)` - جلب راتب حسب الدرجة والمرحلة
- `create(data)` - إضافة درجة ومرحلة جديدة
- `update(id, data)` - تحديث راتب
- `delete(id)` - حذف درجة

### 11. **positionsAPI**
- `getAll(params)` - جلب جميع المناصب
- `create(data)` - إضافة منصب جديد
- `update(id, data)` - تحديث منصب
- `delete(id)` - حذف منصب

### 12. **banksAPI**
- `getAll(params)` - جلب جميع البنوك
- `create(data)` - إضافة بنك جديد
- `update(id, data)` - تحديث بنك
- `delete(id)` - حذف بنك

### 13. **salariesAPI**
- `generate(data)` - احتساب راتب موظف
- `getEmployeeSalary(employeeId, month, year)` - جلب تفاصيل راتب

### 14. **usersAPI**
- `login(credentials)` - تسجيل الدخول
- `register(data)` - تسجيل مستخدم جديد

---

## 🎨 نماذج البيانات (DTOs)

### AddAdministrativeOrderDTO (كتب الشكر)
```javascript
{
  employeeID: number,          // معرف الموظف *
  letterType: number,          // نوع الكتاب (0=شكر، 1=ترفيع) *
  period: number,              // المدة *
  reason: string,              // السبب *
  adminOrderDate: string,      // تاريخ الأمر (ISO 8601) *
  adminOrderNumber: string,    // رقم الأمر *
  periodOrderDate?: string,    // تاريخ أمر المدة (اختياري)
  periodOrderNumber?: string   // رقم أمر المدة (اختياري)
}
```

### CourseRegisterDTO (الدورات)
```javascript
{
  courseName: string,     // اسم الدورة *
  dateStart: string,      // تاريخ البدء (ISO 8601) *
  dateEnd: string,        // تاريخ الانتهاء (ISO 8601) *
  content: string,        // المحتوى *
  teacher: string,        // المدرس *
  fileContent: string,    // محتوى الملف *
  type: string,           // النوع *
  level: string          // المستوى *
}
```

### CourseEmployeeDTO (تسجيل الموظفين في الدورات)
```javascript
{
  courseId: number,      // معرف الدورة *
  employeeId: number,    // معرف الموظف *
  notes?: string,        // ملاحظات (اختياري)
  degree?: number        // الدرجة (اختياري)
}
```

### AddEmployeeDTO (إضافة موظف)
```javascript
{
  firstName: string,              // الاسم الأول *
  secondName: string,             // الاسم الثاني *
  thirdName: string,              // الاسم الثالث *
  fourthName: string,             // الاسم الرابع *
  lastName: string,               // اللقب *
  mothersName: string,            // اسم الأم *
  email?: string,                 // البريد الإلكتروني
  phonenumber: string,            // رقم الهاتف (10+) *
  gender: 0 | 1,                  // الجنس (0=ذكر، 1=أنثى) *
  birthDate: string,              // تاريخ الميلاد *
  maritalStatus: 0 | 1 | 2 | 3,   // الحالة الاجتماعية *
  educationLevelID: number,       // معرف المستوى التعليمي *
  positionID: number,             // معرف المنصب *
  branchID: number,               // معرف الفرع *
  // ... والمزيد من الحقول
}
```

### GenerateSalaryDTO (احتساب الراتب)
```javascript
{
  employeeID: number,    // معرف الموظف *
  month: number,         // الشهر (1-12) *
  year: number,          // السنة *
  notes?: string         // ملاحظات (اختياري)
}
```

---

## 🔐 المصادقة (Authentication)

تتم المصادقة تلقائياً باستخدام Token المخزن في `localStorage`:

```javascript
// التوكن يُضاف تلقائياً لجميع الطلبات
Authorization: Bearer <token>
```

في حالة انتهاء صلاحية التوكن (401):
- يتم حذف التوكن تلقائياً
- إعادة التوجيه إلى صفحة تسجيل الدخول

---

## ⚠️ معالجة الأخطاء

جميع الدوال ترمي أخطاء يمكن اصطيادها:

```javascript
try {
  const data = await api.employees.getAll();
  console.log(data);
} catch (error) {
  console.error('خطأ:', error.message);
  alert('فشل في جلب البيانات: ' + error.message);
}
```

---

## 🚀 الخطوات التالية

### 1. إنشاء صفحات إضافية
يمكنك الآن بسهولة إنشاء صفحات جديدة باستخدام نفس النمط:

- **صفحة إدارة المخصصات والاستقطاعات** - استخدام `entitlementsAPI` و `deductionsAPI`
- **صفحة سلم الرواتب** - استخدام `salaryScaleAPI`
- **صفحة المناصب** - استخدام `positionsAPI`
- **صفحة البنوك** - استخدام `banksAPI`

### 2. ربط الصفحات الموجودة
قم بتحديث الصفحات القديمة لاستخدام `api.js` بدلاً من `fetch` المباشر:

```javascript
// قديم ❌
const res = await fetch('/api/Employee');
const data = await res.json();

// جديد ✅
const data = await api.employees.getAll();
```

### 3. إضافة المزيد من الميزات
- التحقق من صحة البيانات (Validation)
- رسائل نجاح/خطأ أفضل
- Loading states محسّنة
- Caching للبيانات

---

## 🆕 الميزات الجديدة - إدارة الدورات المتقدمة

### 1. **مكون دورات الموظف** (EmployeeCoursesTab)
**المسار:** `src/components/EmployeeCoursesTab.jsx`

**الاستخدام:**
```jsx
import EmployeeCoursesTab from '../components/EmployeeCoursesTab';

// في صفحة تفاصيل الموظف
<EmployeeCoursesTab employeeId={123} />
```

**الميزات:**
- ✅ عرض جميع دورات الموظف
- ✅ إحصائيات سريعة (إجمالي الدورات، المكتملة، معدل الدرجات)
- ✅ تحديث الدرجات والملاحظات
- ✅ حذف تسجيل الموظف من دورة
- ✅ عرض حالة الدورة (لم تبدأ، جارية، منتهية)

### 2. **صفحة تفاصيل الدورة** (CourseDetailsPage)
**المسار:** `src/pages/CourseDetailsPage.jsx`
**الرابط:** `/course-details/:courseId`

**الميزات:**
- ✅ عرض معلومات الدورة الكاملة
- ✅ قائمة جميع الموظفين المسجلين
- ✅ إحصائيات شاملة (عدد الموظفين، معدل الدرجات)
- ✅ تسجيل موظفين جدد في الدورة
- ✅ تحديث درجات وملاحظات الموظفين
- ✅ حذف موظفين من الدورة

### 3. **خدمات CourseEmployee API**
تم إضافة خدمات جديدة في `api.js`:

```javascript
// جلب دورات موظف محدد
const courses = await api.courseEmployee.getByEmployee(employeeId);

// جلب موظفين مسجلين في دورة
const employees = await api.courseEmployee.getByCourse(courseId);

// تحديث درجة وملاحظات
await api.courseEmployee.update(courseEmployeeId, {
  degree: 95,
  notes: 'أداء ممتاز'
});

// حذف تسجيل
await api.courseEmployee.delete(courseEmployeeId);
```

---

## 📋 قائمة المهام المكتملة

### المرحلة الأولى:
- ✅ إنشاء ملف خدمات API مركزي (`api.js`)
- ✅ إنشاء ملف تعريفات الأنواع (`types.js`)
- ✅ بناء صفحة كتب الشكر والترفيعات
- ✅ بناء صفحة إدارة الدورات
- ✅ تحديث HRDashboard لربط الصفحات الجديدة
- ✅ تحديث CourseList.jsx لاستخدام API المركزي
- ✅ تحديث CourseForm.jsx لاستخدام API المركزي

### المرحلة الثانية (الجديدة):
- ✅ إضافة خدمات CourseEmployee API
- ✅ بناء مكون EmployeeCoursesTab
- ✅ بناء صفحة CourseDetailsPage
- ✅ إضافة المسارات الجديدة
- ✅ تحسين CoursesManagement بزر "عرض التفاصيل"

---

## 📞 الدعم والمساعدة

للحصول على مساعدة إضافية:
1. راجع ملف `types.js` لمعرفة جميع الحقول المتاحة
2. راجع ملف `api.js` لمعرفة جميع الدوال المتاحة
3. راجع الصفحات الموجودة (`AppreciationLetters.jsx`, `CoursesManagement.jsx`) كأمثلة عملية

---

## 🎯 الخلاصة

النظام الآن يحتوي على:

1. **طبقة خدمات API موحدة** - سهلة الاستخدام ومنظمة
2. **صفحات جاهزة للاستخدام** - كتب الشكر والدورات وتفاصيل الدورات
3. **مكونات قابلة لإعادة الاستخدام** - EmployeeCoursesTab للموظفين
4. **توثيق كامل** - لجميع النماذج والدوال
5. **معالجة مركزية للأخطاء** - وإعادة المصادقة
6. **واجهات مستخدم عصرية** - مع Tailwind CSS
7. **نظام متكامل لإدارة الدورات** - من الإنشاء إلى التقييم

## 🎓 كيفية استخدام نظام الدورات:

### سير العمل الكامل:

1. **إنشاء دورة** → في `/courses` → إضافة دورة جديدة
2. **تسجيل موظفين** → في `/course-details/{id}` → تسجيل موظف
3. **متابعة الموظف** → في صفحة الموظف → تبويب "الدورات" (EmployeeCoursesTab)
4. **إدخال الدرجات** → في تفاصيل الدورة أو تبويب الموظف
5. **عرض التقارير** → إحصائيات في تفاصيل الدورة

---

**تم بناء هذا النظام بواسطة Claude Code** 🤖
**التاريخ:** 2026-01-18
**آخر تحديث:** 2026-01-18 (إضافة نظام إدارة الدورات المتقدم)
